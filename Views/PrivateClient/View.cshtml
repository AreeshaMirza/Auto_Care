@model AC_Car_Service_.Models.Add

@{
    ViewBag.Title = "View";
}

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>

        </dt>
        <dd>
            <img src="@Url.Content(Model.ImagePath)" width="250" height="250" />
        </dd>
    </dl>
</div>
