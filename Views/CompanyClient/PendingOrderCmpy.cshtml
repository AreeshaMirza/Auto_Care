@{
    ViewBag.Title = "PendingOrderCmpy";
    Layout = "~/Views/Shared/_LayoutClientCmpny.cshtml";
}

<div class="container" style="background-color:white; float:right; width:74%; height:auto; border-radius:7%; margin-right:50px;">
    <h1 style="color:black; font-weight:700; font-family:Gabriola; text-align:left; margin-left:10px; font-size:50px;">Pending Order>Company</h1>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>License Plate</th>
                <th>Chassi Number</th>
                <th>Company List</th>
                <th>Contact Person</th>
                <th>Service Category</th>
                <th>Service</th>
                <th>Choosen Service</th>
                <th>Order Description</th>
                <th>Action</th>
            </tr>
            @foreach (var x in Model)
            {
                <tr>
                    <td>
                        @x.licensePlate
                    </td>
                    <td>
                        @x.chassiNo
                    </td>
                    <td>
                        @x.companyList
                    </td>
                    <td>
                        @x.contactPerson
                    </td>
                    <td>
                        @x.serviceCategory
                    </td>
                    <td>
                        @x.service
                    </td>
                    <td>
                        @x.choosenService
                    </td>
                    <td>
                        @x.orderDescription
                    </td>
                    <td>
                        <div class="btn-group">
                            <button class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                                <span> Action</span>
                            </button>
                            <ul class="dropdown-menu stay-open pull-right" role="menu" style="padding: 15px; min-width: 100px;">
                                <li><a href="/CompanyClient/updatePC/@x.Id">Update</a></li>
                                <li><a href="/CompanyClient/DeleteCmpyPending/@x.Id">Delete</a></li>
                            </ul>
                        </div>
                    </td>

                </tr>
            }
        </thead>
    </table>
</div>
